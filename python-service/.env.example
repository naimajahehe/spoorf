# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Network interface to use (leave empty for auto-detect)
NETWORK_INTERFACE=

# Timeout for ARP requests (seconds)
ARP_TIMEOUT=3

# Interval between spoof packets (seconds)
SPOOF_INTERVAL=2